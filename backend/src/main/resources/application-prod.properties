# Wird nur aktiv, wenn die Umgebungsvariable SPRING_PROFILES_ACTIVE=prod gesetzt ist.
# Reduziert produktions-untaugliche Entwickler-Einstellungen aus application.properties.

spring.jpa.show-sql=false
